# Changelog

## [0.1.0] - 2026-09-05

- Initial version: `SettingsTab`, `ISetting<T>`/`Setting<T>` with Immediate/OnSave apply modes, concrete setting types (float, int, bool, enum, string), `SettingsRegistry`, `ISettingsStore` + `PlayerPrefsJsonSettingsStore`.
