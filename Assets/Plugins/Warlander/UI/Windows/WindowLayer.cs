namespace Warlander.UI.Windows
{
    /// <summary>
    /// When adding new window layers, assigned value matters - higher values will be displayed on top of lower values.
    /// </summary>
    public enum WindowLayer
    {
        Default = 0,
        AlwaysOnTop = 1,
    }
}